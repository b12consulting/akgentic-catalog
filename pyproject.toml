[project]
name = "akgentic-catalog"
version = "1.2.0"
description = "Collaborative agent patterns for the Akgentic framework"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "gpiroux", email = "geoffroy.piroux@weareyuma.com" }
]
keywords = ["ai", "catalog", "collaboration", "pydantic"]
license = "AGPL-3.0-only"
license-files = ["LICENSE"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

# akgentic-* bounds: floor = version on this maintenance line, ceiling = next
# minor. The ceiling is what stops a 1.3.x install from resolving to a 2.x-line
# subpackage release already on PyPI.
dependencies = [
    "pydantic>=2.0.0",
    "pydantic-ai>=1.107.0,<2",
    "pyyaml>=6.0",
    "akgentic-core>=1.3.3,<1.4",
    "akgentic-llm>=1.2.0,<1.3",
    "akgentic-tool>=1.2.2,<1.3",
    "akgentic-team>=1.3.5,<1.4",
]

[project.optional-dependencies]
api = [
    "fastapi>=0.100.0,<0.137",
    "starlette>=0.46.0,<1.1",
    "uvicorn>=0.20.0",
]
cli = [
    "typer>=0.9.0",
    "rich>=13.0.0",
]
mongo = [
    "pymongo>=4.0.0",
]
postgres = [
    "nagra>=0.5",
    "psycopg[binary]>=3.1",
]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.1.0",
    "mypy>=1.8.0",
    "ruff>=0.1.0",
    "types-PyYAML>=6.0",
    "mongomock>=4.0.0",
    "httpx>=0.24.0",
    "nagra>=0.5",
    "psycopg[binary]>=3.1",
    "testcontainers[postgres]>=4.0.0",
    # Test-only: tests import akgentic.agent directly and resolve
    # "akgentic.agent.*" agent_class strings. akgentic-catalog itself must not
    # depend on agent, so it stays out of [project.dependencies].
    "akgentic-agent>=1.2.1,<1.3",
]

[project.scripts]
ak-catalog = "akgentic.catalog.cli.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/akgentic"]

# Explicitly ship non-Python assets needed at runtime (e.g., Nagra schema TOML
# for the Postgres backend). Without this, hatchling's default heuristics may
# skip files like *.toml under src/.
[tool.hatch.build.targets.wheel.force-include]
"src/akgentic/catalog/repositories/postgres/schema.toml" = "akgentic/catalog/repositories/postgres/schema.toml"

# Note: akgentic-* dependencies have NO [tool.uv.sources] entry here.
# - Inside the akgentic-quick-start workspace, the workspace root's
#   [tool.uv.sources] resolves them as editable workspace members.
# - In standalone CI, they resolve from PyPI under the bounds declared above.
# A `akgentic-core = { workspace = true }` entry here is fatal standalone:
# uv rejects a workspace source whose member does not exist.

# Tool configurations inherit from root workspace pyproject.toml
# Only override when package-specific behavior is needed

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.format]
quote-style = "double"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "ANN", "ASYNC"]
ignore = ["ANN401", "N802"]
